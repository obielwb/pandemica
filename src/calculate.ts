import { Activity } from './activities'

/*

eu = red_vacina * red_mascara * age

ambiente = setting  * duration * distance

com covid = (red_mascara * voz * red_vacina * 1   )

*/
export function calculate(activity: Activity) {}
